---
phase: "01-services-nav"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "routes/index.js"
  - "views/services.ejs"
autonomous: true
requirements:
  - "D-01"
  - "D-03"
  - "D-05"
  - "D-07"
  - "UR-01"

must_haves:
  truths:
    - "User visiting /services/ sees grid of enabled services"
    - "Each card links to the actual service page"
    - "Mobile shows single column, desktop shows multi-column grid"
  artifacts:
    - path: "routes/index.js"
      provides: "GET /services/ route handler"
      contains: "router.get('/services'"
    - path: "views/services.ejs"
      provides: "Service grid page template"
      min_lines: 50
---

<objective>
Create user-facing service directory page at /services/ with responsive grid layout.
</objective>

<context>
@.planning/phases/01-services-nav/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@routes/index.js
@views/coupon.ejs
@utils/services-loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /services/ route</name>
  <files>routes/index.js</files>
  <read_first>routes/index.js</read_first>
  <action>
Add the following route to routes/index.js after the existing routes (before module.exports):

```javascript
const servicesLoader = require('../utils/services-loader');

router.get('/services/', (req, res) => {
  const userId = getUserId(req, res);
  statistics.recordVisit(userId, '/services/');
  const services = servicesLoader.getEnabledServices();
  res.render('services', { services });
});
```

Per D-01: Route `/services/` — user grid card interface
Per UR-01: Route returns HTML page with service cards
Per UR-01: Statistics recorded for page visits

Verify: router.get('/services/' exists in routes/index.js
</action>
  <verify>
grep -q "router.get('/services'" routes/index.js
grep -q "servicesLoader.getEnabledServices" routes/index.js
grep -q "statistics.recordVisit" routes/index.js
  </verify>
  <done>/services/ route returns enabled services sorted by order</done>
</task>

<task type="auto">
  <name>Task 2: Create services.ejs view</name>
  <files>views/services.ejs</files>
  <read_first>views/coupon.ejs</read_first>
  <action>
Create views/services.ejs following coupon.ejs patterns:

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>服務總覽</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
        .fade-in {
            animation: fadeIn 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="bg-slate-50 min-h-screen md:py-12">

    <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="text-center py-8 md:py-12">
            <h1 class="text-3xl md:text-4xl font-bold text-gray-800 tracking-tight">服務總覽</h1>
            <p class="mt-2 text-gray-400 text-sm md:text-base">選擇您需要的服務</p>
        </div>

        <!-- Service Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6 px-4 pb-12">
            <% services.forEach(function(service, index) { %>
            <a href="<%= service.path %>" 
               class="block bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-md hover:border-gray-200 transition-all group fade-in"
               style="animation-delay: <%= index * 0.1 %>s">
                <div class="aspect-video bg-gradient-to-br from-blue-50 to-indigo-50 flex items-center justify-center">
                    <svg class="w-12 h-12 text-blue-400 group-hover:text-blue-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <% if (service.icon === 'calendar') { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                        <% } else if (service.icon === 'ticket') { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z"></path>
                        <% } else if (service.icon === 'qrcode') { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h2M4 12h4m12 0h.01M5 8h2a1 1 0 001-1V5a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1zm12 0h2a1 1 0 001-1V5a1 1 0 00-1-1h-2a1 1 0 00-1 1v2a1 1 0 001 1zM5 20h2a1 1 0 001-1v-2a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1z"></path>
                        <% } else { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        <% } %>
                    </svg>
                </div>
                <div class="p-4 md:p-5">
                    <h3 class="text-lg font-bold text-gray-800 mb-1"><%= service.name %></h3>
                    <p class="text-gray-400 text-sm line-clamp-2"><%= service.description %></p>
                </div>
            </a>
            <% }); %>
        </div>
    </div>

</body>
</html>
```

Key implementation details:
- Per D-05: Minimalist design, clear typography, generous whitespace
- Per D-07: Responsive grid — `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (mobile single column, desktop 2-3 columns)
- Per D-08: Card shows name, description, icon
- Per UR-01: Cards link to actual service pages (service.path)
- Uses existing Tailwind patterns: `rounded-2xl`, `shadow-sm`, `border`, `hover:shadow-md`
- Matches coupon.ejs styling: `bg-slate-50`, `text-gray-400`, low-contrast palette
- Fade-in animation per existing patterns in coupon.ejs and 1.ejs
</action>
  <verify>
file exists with services.ejs content
grid layout uses: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
each card links to service.path
  </verify>
  <done>Services page displays enabled services in responsive grid</done>
</task>

</tasks>

<success_criteria>
- GET /services/ returns HTML with service cards
- Services displayed in responsive grid (1 col mobile, 2-3 cols desktop)
- Each card shows name, description, icon and links to actual service path
- Statistics recorded on page visit
</success_criteria>

<output>
After completion, create `.planning/phases/01-services-nav/01-02-SUMMARY.md`
</output>