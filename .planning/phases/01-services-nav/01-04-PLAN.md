---
phase: "01-services-nav"
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified: []
autonomous: true
requirements:
  - "NFR-04"

must_haves:
  truths:
    - "All existing routes continue to work"
    - "New routes work correctly"
    - "Service configuration loads properly"
  artifacts:
    - path: "data/services.json"
      provides: "Service configuration exists and valid"
---

<objective>
Verify all existing functionality and new routes work correctly.
</objective>

<context>
@.planning/phases/01-services-nav/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@routes/index.js
@data/services.json
@utils/services-loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing routes still work</name>
  <files>routes/index.js</files>
  <action>
Run the application and test existing routes:

```bash
npm run dev &
sleep 3
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/public/1
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/public/coupon
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/public/url-qr-doc-tool
```

Existing routes must still return 200:
- /public/1 → 200
- /public/coupon → 200
- /public/url-qr-doc-tool → 200

Per NFR-04: coupon.ejs remains unchanged, existing routes unaffected
</action>
  <verify>
each existing route returns HTTP 200
pages render without errors in browser
  </verify>
  <done>All existing routes continue to work</done>
</task>

<task type="auto">
  <name>Task 2: Verify new routes work</name>
  <files>routes/index.js, views/services.ejs, views/admin/services.ejs</files>
  <action>
Test new routes:

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/services/
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/mgmt/services
```

New routes must return 200:
- /services/ → 200
- /mgmt/services → 200

Verify content:
- /services/ contains service cards with links
- /mgmt/services contains admin controls
</action>
  <verify>
/services/ returns HTTP 200
/mgmt/services returns HTTP 200
pages contain expected content
  </verify>
  <done>New routes work correctly</done>
</task>

<task type="auto">
  <name>Task 3: Verify service loader works</name>
  <files>utils/services-loader.js, data/services.json</files>
  <action>
Test the service loader:

```bash
node -e "const loader = require('./utils/services-loader'); const services = loader.getServices(); console.log('Services:', services.length); console.log('Enabled:', loader.getEnabledServices().length);"
```

Expected output:
- Services: 3
- Enabled: 3

Verify data/services.json is valid JSON and contains service structure per plan 01-01.
</action>
  <verify>
node script runs without errors
returns expected service count
  </verify>
  <done>Service loader utility functions correctly</done>
</task>

</tasks>

<success_criteria>
- All existing routes return 200: /public/1, /public/coupon, /public/url-qr-doc-tool
- All new routes return 200: /services/, /mgmt/services
- Service loader returns 3 services
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-services-nav/01-04-SUMMARY.md`
</output>