---
phase: 04-services-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - DB-05
  - DB-06
  - DB-07
  - DB-08

must_haves:
  truths:
    - "All database modules exported from single index.js"
    - "Migration modules accessible for data loading"
    - "Easy import: require('./utils/db') gets all modules"
  artifacts:
    - path: "utils/db/index.js"
      provides: "Unified database exports"
      exports: ["courses", "services", "statistics", "connection"]
  key_links:
    - from: "utils/db/index.js"
      to: "utils/db/courses.js"
      via: "require"
    - from: "utils/db/index.js"
      to: "utils/db/services.js"
      via: "require"
    - from: "utils/db/index.js"
      to: "utils/db/statistics.js"
      via: "require"
    - from: "utils/db/index.js"
      to: "utils/db/connection.js"
      via: "require"
---

<objective>
Create unified data access module that exports all database functions.

Purpose: Provide a single entry point for importing all database modules, making it easier to require and use the data access layer.

Output: Unified index.js at utils/db/index.js
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

# Existing database modules
@utils/db/courses.js
@utils/db/services.js
@utils/db/statistics.js
@utils/db/connection.js

# Migration modules
@utils/db/schema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified data access module</name>
  <files>utils/db/index.js</files>
  <action>
Create utils/db/index.js that exports all database modules and provides convenience functions:

Exports:
- connection: { open, close, get, getDbPath } from './connection'
- schema: { createTables, dropTables, initialize } from './schema'
- courses: all exports from './courses'
- services: all exports from './services'
- statistics: all exports from './statistics'

Also provide convenience function:
- initializeAll(): Calls schema.initialize() to ensure all tables exist
- migrateAll(jsonDir): Runs migrateFromJson for courses, services, statistics

This allows simple import: const db = require('./utils/db');
  </action>
  <verify>
<automated>node -e "const db=require('./utils/db'); console.log('Exports:', Object.keys(db));"</automated>
  </verify>
  <done>index.js exports connection, schema, courses, services, statistics modules</done>
</task>

<task type="auto">
  <name>Task 2: Initialize and verify all migrations</name>
  <files>utils/db/index.js</files>
  <action>
Initialize database and run all migrations to ensure data is loaded:

1. Call db.initializeAll() to create all tables (if not already created)
2. Run courses migration if data exists in data/courses.json (using courses.migrateFromJson)
3. Run services migration if data exists in data/services.json (using services.migrateFromJson)
4. Run statistics migration if data exists in data/*.json (using statistics.migrateFromJson)

Verify each module has data after migration by checking counts.
  </action>
  <verify>
<automated>node -e "const db=require('./utils/db'); const c=db.courses.getAll().length; const s=db.services.getAll().length; console.log('Courses:',c,'Services:',s);"</automated>
  </verify>
  <done>All database modules initialized and data migrated successfully</done>
</task>

</tasks>

<verification>
# Overall phase checks

1. Unified index.js exports all modules
2. Database properly initialized
3. All data migrated from JSON to SQLite
4. Modules can be imported and used
</verification>

<success_criteria>
Measurable completion:
- [ ] utils/db/index.js created
- [ ] All modules exported and accessible
- [ ] Database initialized with all tables
- [ ] Data migrated from JSON files
- [ ] No errors when importing
</success_criteria>

<output>
After completion, create .planning/phases/04-services-migration/04-02-SUMMARY.md
</output>